Here is my projectcard.astro
---
import { ArrowRight } from "lucide-react";
import settings from "../../config/settings.yaml";
const { data } = Astro.props;
const { data: project } = data;
const cover = `${settings.base_url}/project-images/${project.cover}`;
const url = `${settings.base_url}/projects/${project.slug}`;
---

<a
    href={url}
    data-category={project.category.join("|")}
    data-year={project.year}
    class="group border rounded-box border-base-100/20 flex flex-col cursor-pointer justify-center gap-4 items-start"
>
    <div class="overflow-hidden bg-white rounded-t-box w-full">
        <img
            src={cover}
            alt={project.title}
            loading="lazy"
            class="w-full aspect-video object-contain align-top transition-transform duration-500 transform group-hover:scale-105"
        />
    </div>

    <div class="p-4 space-y-4 w-full">
        <h3 class="text-xl line-clamp-2 min-h-[3.25rem] text-primary font-bold">
            {project.title}
        </h3>
        <p class="text-base-300 text-sm line-clamp-3 min-h-[4.00rem]">{project.description}</p>

        <div class="relative h-7 w-full overflow-hidden">
            <p
                class="absolute transition-all duration-500 text-base-300 text-xs uppercase transform group-hover:-translate-y-full tracking-normal [font-feature-settings:normal] flex items-center justify-between gap-2 w-full"
            >
                <span class="truncate">{project.category.join(", ")}</span>
                <span class="shrink-0 text-primary font-bold">{project.year}</span>
            </p>
            <button
                class="absolute transition-all duration-500 transform translate-y-full group-hover:translate-y-0 text-primary font-bold w-full"
                aria-label={`View project: ${project.title}`}
            >
                <div class="flex gap-3 items-center justify-start">
                    <div>View Project</div><ArrowRight />
                </div>
            </button>
        </div>
    </div>
</a>
